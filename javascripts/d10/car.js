 export class Car {
     constructor(model, year) {
         this.model = model;
         this.year = year;

     }

     toString() {
         return `model :${this.model},year:${this.year}`;
     }
 }


 //export default Car;
 //export { Car };


 // let car_1 = new Car ("pmw ", 2020);

 // let flying1 = new FlyingCar("toyota",2003, false);